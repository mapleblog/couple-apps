rules_version = '2';

// 临时完全公开的Storage规则用于调试
service firebase.storage {
  match /b/{bucket}/o {
    // 临时允许所有人读取照片（仅用于调试）
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
  }
}